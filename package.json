{
  "name": "mcphealth",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @mcphealth/api dev",
    "dev:web": "pnpm --filter @mcphealth/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
